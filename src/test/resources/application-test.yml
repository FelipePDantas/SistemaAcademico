spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/sistema_academico_test
    username: postgres
    password: postgres
  jackson:
    default-property-inclusion: non_null
